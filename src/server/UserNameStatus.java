package server;

/**
 * Enum representing the possible status of a user's chosen username.
 * 
 * - `EMPTY`: Indicates the username is available.
 * - `CHOSEN`: Indicates the username has already been selected.
 */
public enum UserNameStatus {
	EMPTY,
	CHOSEN
}
